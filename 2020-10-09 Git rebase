pick ：不做任何修改；(表明正在使用；)

reword：只修改提交注释信息；(表明仍然使用该提交对象，但是需修改提交信息；)

edit：修改提交的文件，做增补提交；(使用该提交对象，但是不合并提交对象；)

squash：将该条提交合并到上一条提交，提交注释也一并合并；(使用该提交对象，但是将此提交与上一次提交对象合并；)

fixup：将该条提交合并到上一条提交，废弃该条提交的注释；(同squash值，但是丢失此次提交的日志信息；)

exec(x)，后接特定脚本，保存后将执行该脚本；

drop(d)，移除该提交对象
